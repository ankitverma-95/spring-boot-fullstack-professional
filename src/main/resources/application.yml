server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

spring:
  profiles:
    active: staging
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?user=postgres.pgrardklsorfdnkmewhi&password=Supabase@12345&prepareThreshold=0
    username: postgres.pgrardklsorfdnkmewhi
    password: Supabase@12345
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  redis:
    host: localhost
    port: 6379
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
      shutdown-timeout: 100ms
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100

app:
  cors:
    allowed-origins: http://localhost:3000
  wage-api:
    url: https://api.mockgovernment.gov/wage-rates
    timeout-ms: 3000
